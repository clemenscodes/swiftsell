import { NextButton } from './next-button';
import type { Meta } from '@storybook/react';

const Story: Meta<typeof NextButton> = {
    component: NextButton,
    title: 'NextButton',
};
export default Story;

export const Primary = {
    args: {},
};
