import { ProfileButton } from './profile-button';
import type { Meta } from '@storybook/react';

const Story: Meta<typeof ProfileButton> = {
    component: ProfileButton,
    title: 'ProfileButton',
};
export default Story;

export const Primary = {
    args: {},
};
