import { DotButton } from './dot-button';
import type { Meta } from '@storybook/react';

const Story: Meta<typeof DotButton> = {
    component: DotButton,
    title: 'DotButton',
};
export default Story;

export const Primary = {
    args: {},
};
