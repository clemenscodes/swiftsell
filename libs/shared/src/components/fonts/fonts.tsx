import { cn } from '@styles';
/* eslint-disable-next-line */
export interface FontsProps {}

export const Fonts: React.FC<FontsProps> = ({ ...props }) => {
    return (
        <div className={cn([])}>
            <h1>Welcome to Fonts!</h1>
        </div>
    );
};

export default Fonts;
